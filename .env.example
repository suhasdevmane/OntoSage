# Example environment file for docker-compose (DO NOT COMMIT real secrets)
# Copy this file to `.env` and fill in real values on the host or CI secret store.
# This repository's .gitignore already excludes `.env` files.

# Example variables used by docker-compose services
DB_USERNAME=your_db_username_here
DB_PASSWORD=your_db_password_here
DB_HOST=localhost
DB_PORT=5432

REDIS_PASSWORD=change_me
REDIS_HOST=redis
REDIS_PORT=6379

THINGSBOARD_API_KEY=replace_with_key
SOME_SERVICE_USER=service_user
SOME_SERVICE_PASS=service_pass

# API / database placeholders (used by docker-compose.bldg1.yml)
# API Postgres connection (api service)
API_PGHOST=postgres
API_PGPORT=5432
API_PGUSER=postgres
API_PGPASSWORD=postgres
API_PGDATABASE=abacws

# API MySQL fallback (if DB_ENGINE=mysql)
API_MYSQL_HOST=mysql
API_MYSQL_PORT=3306
API_MYSQL_USER=root
API_MYSQL_PASSWORD=mysql
API_MYSQL_DATABASE=abacws

# API auth / service keys
API_KEY=change_me

# Action server DB credentials (local/dev)
ACTION_DB_USER=root
ACTION_DB_PASSWORD=mysql

# HTTP server secrets
JWT_SECRET=change_me_in_prod

# ThingsBoard/Postgres service credentials
PG_THINGSBOARD_DB=thingsboard
PG_THINGSBOARD_USER=thingsboard
PG_THINGSBOARD_PASSWORD=thingsboard

# MySQL service (legacy) credentials
MYSQL_ROOT_PASSWORD=mysql
DB_NAME=sensordb
MYSQL_APP_USER=thingsboard
MYSQL_APP_PASSWORD=thingsboard

# Fuseki admin password placeholder
FUSEKI_ADMIN_PASSWORD=Admin@12345

# PGAdmin UI defaults (placeholders)
PGADMIN_DEFAULT_EMAIL=pgadmin@example.com
PGADMIN_DEFAULT_PASSWORD=admin

# Generic DB connection placeholders (used by action_server/builds)
DB_USER=root
DB_PASSWORD=mysql

# Frontend/origins
FRONTEND_ORIGIN=http://localhost:3000
ALLOWED_ORIGINS=http://localhost:3000

# Notes:
# - Keep only placeholders here. Never store production secrets in the repository.
# - Prefer using OS-level environment variables, Docker secrets, or your cloud provider's
#   secret manager in CI/CD rather than a plaintext .env file.
