"""
deliberation — the ARBITER deliberative layer (Improvement Plan V4).

Every module in this package is building-agnostic: zero building literals, enforced
by tests/test_coverage_audit.py::test_no_building_literals_in_deliberation_modules.

Modules land phase by phase (see tasks/V4_TRACKER.csv):
  coverage_audit.py   V4-T06  space x modality coverage matrix (SATURATE input)
"""
