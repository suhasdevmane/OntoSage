# Portability Harness Config for Building B (TimescaleDB)
endpoints:
  rasa_url: http://localhost:5005
  t5_url: http://localhost:8008/translate
  fuseki:
    url: http://localhost:3030/ds/query
  analytics_base_url: http://localhost:8080

resources:
  # Optional labels to help entity normalisation in dry-run
  labels:
    - co2 sensor
    - room 5.12
    - zone air temperature
    - air quality sensor
    - pm10 sensor
    - hvac
    - supply temperature
  # T2 alias rules (applied to question text before extraction)
  alias_rules: evaluation/config/alias_rules.sample.json
