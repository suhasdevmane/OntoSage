# Service Catalog

This document describes every service in **OntoSage**, including responsibilities, ports, environment, health checks, dependencies, and interactions.

## Overview Diagram

```mermaid
graph LR
  subgraph UI
    FE[Open WebUI]
  end
  subgraph Core
    ORCH[Orchestrator]
    REDIS[(Redis)]
  end
  subgraph Knowledge
    GDB[(GraphDB)]
    QDR[Qdrant]
    RAG[RAG Service]
  end
  subgraph Compute
    EXEC[Code Executor]
    OLLA[Ollama]
  end
  subgraph Data
    MYSQL[(MySQL)]
  end

  FE -- REST/WS --> ORCH
  ORCH -- cache --> REDIS
  ORCH -- RAG ctx --> RAG
  RAG -- vectors --> QDR
  RAG -- SPARQL/REST --> GDB
  ORCH -- exec code --> EXEC
  ORCH -. inference .-> OLLA
  ORCH -- SQL --> MYSQL
```

---

## Orchestrator (FastAPI + LangGraph)
- **Port**: `8000`
- **Image/Build**: `orchestrator/Dockerfile`
- **Mounts**: `./orchestrator`, `./shared` (hot reload), `./data/bldg1`, `./outputs/query_results`
- **Env (key)**: `REDIS_HOST`, `RAG_SERVICE_HOST`, `CODE_EXECUTOR_HOST`, `GRAPHDB_HOST`, `MYSQL_HOST`, model provider vars
- **Health**: `GET /health`
- **Depends**: `redis`, `graphdb-rag-service`, `code-executor`, `graphdb`, `mysql`, `ollama`
- **Duties**:
  - **Cognitive Core**: Manages conversation state and tool routing using LangGraph.
  - **Agent Graph**: Coordinates Dialogue, SPARQL, SQL, Analytics, and Visualization agents.
  - **Provider Abstraction**: Seamlessly switches between Ollama (Local) and OpenAI (Cloud).
  - **API Standardization**: All endpoints return a uniform JSON structure (`{ success, data, error }`).

## Open WebUI (Frontend & Voice)
- **Port**: `3001` (mapped to container 8080)
- **Image**: `ghcr.io/open-webui/open-webui:main`
- **Duties**:
  - **User Interface**: Provides a rich, chat-based interface for users.
  - **Voice Interaction**: Handles Speech-to-Text (STT) and Text-to-Speech (TTS) natively.
  - **Visualization**: Renders Markdown and interactive plots generated by the backend.

## RAG Service (GraphDB Native)
- **Port**: `8001`
- **Image/Build**: `rag-service/graphdbRAG/Dockerfile`
- **Env (key)**: `GRAPHDB_REPOSITORY`, `GRAPHDB_SIMILARITY_INDEX`
- **Health**: `GET /health`
- **Depends**: `graphdb`
- **Duties**:
  - **Semantic Search**: Uses **GraphDB Similarity Indexing** to find ontology entities matching user queries.
  - **Context Injection**: Retrieves "bounded context" (neighboring triples) from the Knowledge Graph to ground LLM responses.
  - **No External Vector DB**: Operates entirely within the GraphDB ecosystem for ontology mapping.

## GraphDB
- **Ports**: `7200` (HTTP), `7300` (gRPC) [bound to localhost]
- **Volumes**: `./volumes/graphdb:/opt/graphdb/home`, `./data/bldg1/trial/dataset:/opt/graphdb/import:ro`
- **Env**: `GDB_HEAP_SIZE`, `GDB_MAX_MEM`
- **Health**: `GET /rest/repositories`
- **Duties**: 
  - **Ontology Store**: Hosts the RDF Knowledge Graph representing the building topology.
  - **Similarity Index**: Manages the vector index for semantic search.
  - **SPARQL Endpoint**: Executes semantic queries generated by the SPARQL Agent.

## Qdrant (Memory Store)
- **Ports**: `6333` (HTTP), `6334` (gRPC)
- **Volume**: `./volumes/qdrant:/qdrant/storage`
- **Health**: `GET /health`
- **Duties**: 
  - **Long-Term Memory**: Stores vector embeddings of **User Conversation History**.
  - **Context Retrieval**: Allows the Orchestrator to fetch relevant past interactions to maintain conversation continuity.

## Code Executor
- **Port**: `8002`
- **Build**: `code-executor/Dockerfile`
- **Env**: from `.env` (timeout/limits via shared config)
- **Health**: `GET /health`
- **Duties**: 
  - **Secure Sandbox**: Executes Python code generated by the Analytics Agent in an isolated environment.
  - **Safety**: Prevents unauthorized network access or file system modifications.

## Whisper STT
- Port: host `8003` -> container `10300`
- Image: `lscr.io/linuxserver/faster-whisper`
- Env: `WHISPER_MODEL`, `WHISPER_BEAM`, `WHISPER_LANG`
- Health: `GET /health` on `10300`
- Duties: Audio transcription

## Ollama (Local LLM)
- Port: host `11435` -> container `11434`
- Volume: `./volumes/ollama:/root/.ollama`
- Health: `ollama list`
- Duties: Local model serving (e.g., `deepseek-r1:32b`)

## Redis (Memory Store)
- Port: `6379`
- Volume: `./volumes/redis`
- Command: `--appendonly yes --maxmemory 2gb --maxmemory-policy allkeys-lru`
- Health: `redis-cli ping`
- Duties: Conversation and agent state

## MySQL (Sensor Time-Series)
- Port: host `3307` -> container `3306`
- Volume: `mysql-data:/var/lib/mysql` (external)
- Health: `mysqladmin ping`
- Duties: Historical telemetry, sensor UUID mappings

## Postgres (User Data)
- Port: host `5433` -> container `5432`
- Volume: `./volumes/postgres-user-data`
- Health: `pg_isready`
- Duties: User chat history, UI data (optional)

## Mongo (Chat History)
- Port: `27017`
- Volume: `./volumes/mongo`
- Health: `mongosh db.adminCommand('ping')`
- Duties: Chat/session data for legacy UI and integrations

## Abacws API & Visualiser (Legacy/Integration)
- API Port: `5000`; Visualiser Port: `8090`
- Build: `Abacws/api` and `Abacws/visualiser`
- Duties: 3D digital twin and REST backend used by visualiser

## File Server (Artifacts)
- Port: `8080`
- Volume: `./volumes/artifacts`
- Duties: Serve generated plots/images/artifacts via static hosting

## Monitoring (Optional)
- Prometheus: `9090`; Grafana: `3001`
- Duties: Metrics collection and dashboards (enable with `--profile monitoring`)
