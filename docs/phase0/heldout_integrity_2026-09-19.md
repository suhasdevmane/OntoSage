# Held-out integrity log (kept by the lead; goes into the final readiness document)

VERBATIM leaks (question text found in code/tests before the final run):
- tail C item 34  "what design choices?"  -> Agent J test (lead quoted it in the brief)
- tail D item 23  "Is the ambient light sufficient?" -> Agent K test example (pool contained held-out rows)
- tail D item 47  "can the system suggest improvements for better recycling habits?" -> pre-existing scripts/ttl_gap_audit.py
=> excluded from the headline of their sets.

CLASS-LEVEL exposure (lead's briefs paraphrased these tail C failures to Agents C and J, who built fixes for the class):
- C2 sound-proofing -> live-figures block; C3 access-feed freshness -> 'designed/built by'; C14 CO2 sensor cross-sensitivity refused
  as 'reaches N sensors'; C20 aggregation methodology -> unrelated quote; C21 'how to control CO2'; C33 BREEAM/LEED; C34 design
  choices (also verbatim); C48 asset duplicates; C44/C61 knowledge questions refused as 'reaches N sensors'.
=> tail C delta is reported as an UPPER BOUND; tail D (never read by anyone before the final run, 2 verbatim leaks excluded) is the
   primary unscripted measure.
